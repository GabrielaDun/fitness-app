import styles from './Checkout.module.scss'

const Checkout = () => {
    return (
        <div>
            <h1 className={styles.title}>Checkout</h1>
        </div>
    );
};

export default Checkout;